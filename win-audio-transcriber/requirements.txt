# Windows-only: PyAudio fork with WASAPI loopback support
PyAudioWPatch>=0.2.12; sys_platform == "win32"

# Local speech-to-text (CTranslate2 Whisper implementation, CPU or CUDA)
faster-whisper>=1.0.0
